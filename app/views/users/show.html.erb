<% if Block.exists?(blocking_id: current_user.id, blocked_id: params[:id]) %>
<%= render 'blocking' %>
<% elsif Block.exists?(blocking_id: params[:id], blocked_id: current_user.id) %>
<%= render 'blocked' %>
<% else %>
<%= render 'show_common' %>
<% end %>