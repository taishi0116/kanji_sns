<li>
  <% if user.image? %>
  <%= link_to image_tag(user.image.thumb.url, :size => '50x50'), user %>
  <% else %>
  <%= link_to image_tag('default.png', :size => '50x50'), user %>
  <% end %>
  <%= link_to user.name, user %>
</li>